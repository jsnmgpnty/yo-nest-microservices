import { EntityMetadataTransformerInterceptor } from './entity-metadata-transformer.interceptor';

describe('EntityMetadataTransformerInterceptor', () => {
  it('should be defined', () => {
    expect(new EntityMetadataTransformerInterceptor()).toBeDefined();
  });
});
